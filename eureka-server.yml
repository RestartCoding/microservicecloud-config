server:
  port: 8082
spring:
  application:
    name: hystrix-service
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/
  server:
  instance:
    prefer-ip-address: true